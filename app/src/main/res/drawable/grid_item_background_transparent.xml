<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="#F44336" /> <!-- 50% transparent purple -->
            <corners android:radius="8dp" />
        </shape>
    </item>

    <!-- Default state -->
    <item>
        <shape>
            <solid android:color="#FFC107" /> <!-- Fully transparent background -->
            <corners android:radius="8dp" />
            <stroke android:width="2dp" android:color="#F44336" /> <!-- Opaque stroke -->
        </shape>
    </item>
</selector>
